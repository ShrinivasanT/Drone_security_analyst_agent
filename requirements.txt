# Backend / API
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
pydantic==2.10.4
python-multipart==0.0.20   # multipart/form-data parsing for FastAPI UploadFile (POST /ingest_video)

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pgvector==0.3.6

# Blob storage (MinIO via S3 API)
boto3==1.35.97

# Agent framework
langgraph==0.2.62
langchain-core==0.3.29   # >=0.3.29 required by langchain-openai 0.3.0; allowed by langgraph 0.2.62
langchain-openai==0.3.0

# VLM / embeddings client (OpenAI GPT-4o + text-embedding-3-small)
openai==1.59.6

# Video ingestion — sample frames from source video clips
opencv-python-headless==4.10.0.84   # cv2.VideoCapture: open clips, sample frames at an interval (no GUI deps, Docker-friendly)
pillow==11.1.0                       # encode sampled frames as JPEG bytes

# Testing
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
